```{r load-data}
#| label: load-pkgs
#| message: false
#| echo: FALSE
#| warning: FALSE

library(tidyverse)
library(bakeoff)
library(dplyr)

```
```{r}
gb_cities <- 
  read_csv(file="data/gb.csv")
```

```{r}

new_bakers <-
  left_join(bakers, gb_cities, by=c("hometown"="city")
)
sum(is.na(new_bakers$lat))


```
```{r}
library(ggridges)

library(scales)
```

```{r}
new_ratings <-
  ratings |>
  mutate(episode = as.factor(episode))

ggplot(new_ratings, aes(x=viewers_7day, y=episode))+
  geom_density_ridges(scale=1, rel_min_height = 0.01)
```

